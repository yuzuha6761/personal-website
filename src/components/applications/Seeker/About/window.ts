import type { WindowDisplayOptions } from '~types'

export const aboutWindowOptions = {
  fullSizeContentView: false,
  zoomDisabled: true,
  resizable: false,
  size: {
    width: 21.5,
    height: 23.5,
  },
} satisfies WindowDisplayOptions
